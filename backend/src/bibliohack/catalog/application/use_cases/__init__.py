"""catalog — application use cases.

Each module here orchestrates the domain via the application-layer ports.
No infrastructure imports allowed — concrete I/O lives in `infrastructure/`,
and use cases receive ports as constructor arguments.
"""
